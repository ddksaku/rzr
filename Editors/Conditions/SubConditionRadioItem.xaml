<UserControl x:Class="Rzr.Core.Editors.Conditions.SubConditionRadioItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Path=Name}" Foreground="White" TextWrapping="Wrap" />
        <RadioButton Grid.Row="0" Grid.Column="2" IsChecked="{Binding Path=IsSelected}" Margin="4,5,4,0" GroupName="{Binding Path=GroupName}"/>
    </Grid>
</UserControl>
